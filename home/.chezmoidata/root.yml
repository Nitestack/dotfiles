---
fonts:
  display: "Geist"
  mono: "SF Mono"
  icons: "Symbols Nerd Font"
dependencies:
  all:
    packages:
      - "git"
      - "vim"
      - "ripgrep"
      - "unzip"
    pnpm:
      - "neovim"
      - "tree-sitter-cli"
      - "@nitestack/project-manager"
      - "@bitwarden/cli"
      - "npkill"
      - "turbo"
      - "vercel"
    cargo:
      - "bob-nvim"
      - "nvim-switcher"
    custom:
      - nvim:
          - "bob install stable"
          - "bob install nightly"
          - "bob use stable"
      - pnpm:
          - "npm install -g pnpm"
    manual: []
  unix:
    packages:
      - "curl"
      - "tmux"
      - "exa"
    custom:
      - bashly:
          - "sudo gem install bashly"
      - node:
          - "nvm install --lts"
    manual: []
