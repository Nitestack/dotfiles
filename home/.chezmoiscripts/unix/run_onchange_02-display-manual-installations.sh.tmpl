#!/usr/bin/env bash

# {{ template "bash-library.sh" . }}

# The following line is for ShellCheck to correctly identify the above included library
true || source "../../.chezmoitemplates/bash-library.sh"

# {{ if eq .chezmoi.os "darwin" }}
log_info "macOS: You have to manually install the following packages:"
# {{ range (concat .dependencies.macos.manual .dependencies.unix.manual .dependencies.all.manual | sortAlpha | uniq) }}
echo "- {{ . -}}"
# {{ end }}
# {{ end }}
